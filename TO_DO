inserisci opzione per scegliere tra ising 0,1 // ising +-1 // potts generalizzato -> ho scritto diverse funzioni (OK)

Inserisci funzione per calcolare magnetizzazione // correlazione // stampa energia

Funzione (e opzione) per calcolare vicini (se tutto connnesso non serve, pero inserisci)

PARALLELIZZARE

Scrivi mapping tra le varie hamiltoniane (ising 01, ising pm1 [vedi pdf SC e FZ], potts generalizzato)

scrivi funzione per controllare input

(implementa altri oltre a metropolis? (e.g. cluster))
